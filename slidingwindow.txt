
import java.util.io.*;
import java.util.Scanner;
class slidingwindow{
	public static int slidingwindow(int array[])
	{
		int sum=0,max,k;
		for(int i=0;i<k;i++)
		{
			sum=sum+a[i];
		}
		max=sum;
		for(int i=k;i<n;i++)
		{
			sum=sum+a[i]-a[i-k];
		}
		return Math.max(array,sum);
	}
	return max;
}
		
